#include "ZU.hpp"

std::ostream& operator<<(std::ostream& os, const ZoneUrbaine& zu){
  os << "Zone Urbaine : " << std::endl;
    //os << "Type : " << typeid(*this).name() << endl;
    //os << "Polygone : " << zu.forme << endl;
    //os << "Proprietaire : " << zu.proprietaire << std::endl;
    //os << "Surface : " << zu.surface << std::endl;
    //os << "% Constructible : " << zu.pConstructible << std::endl;
    //os << "Surface construite : " << zu.surfaceConstruite << std::endl;
    //os << "Surface à constuire restante : " << (zu.surface*zu.pConstructible)-zu.surfaceConstruite << endl;
    return os;
}